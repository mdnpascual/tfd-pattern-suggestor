import React from 'react';
import { Box } from '@mui/material';

const InventoryComponent: React.FC = () => {
	return <Box p={3}>Content for Inventory (WIP)</Box>;
};

export default InventoryComponent;